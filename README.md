# A little something for you

A framework-free, static romantic scrapbook website. Open `index.html` directly in a browser or deploy the folder to GitHub Pages.

Editable content lives in `config.js`. Local photos are in `assets/photos/` and the voice note is in `assets/voice/`.
